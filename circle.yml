test:
  override:
    - echo ${USERNAME}
    - echo ${PASSWORD}
    - ant -f build/build.xml -lib build/lib/
  timeout: 600

deployment:
  production:
    branch: master
    commands:
      - ant deployCode
